events {
    worker_connections 768;
}

http {

    server {
      listen 80;
      
      location / {
        proxy_pass http://server:8000;
      }

      location /static/ {
        alias /app/public/static/;
      }
  }
}